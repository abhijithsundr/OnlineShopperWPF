<Window x:Class="RockstarTHA.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:RockstarTHA.WPF.Controls"
        xmlns:local="clr-namespace:RockstarTHA.WPF"
        mc:Ignorable="d"
        Title="Online Shopper" Height="450" Width="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Set the data context passed to Navigationbar -->
        <controls:Navigationbar Grid.Row="0" DataContext="{Binding Navigator}" />
        
        <!--- Set the content for the tab -->
        <ContentControl Grid.Row="1" Content="{Binding Navigator.Current}" />
    </Grid>    

</Window>
